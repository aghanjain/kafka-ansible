---
all:
  children:
    zookeeper_servers:
      hosts:
        # We list all three nodes here for the ZooKeeper ensemble
        kafka-node-0:
          ansible_host: 10.0.0.50
          zookeeper_myid: 1
        kafka-node-1:
          ansible_host: 10.0.0.43
          zookeeper_myid: 2
#        kafka-node-2:
#          ansible_host: 10.0.0.60
#          zookeeper_myid: 3
    kafka_servers:
      hosts:
        # We list all three nodes here for the Kafka cluster
        kafka-node-0:
          ansible_host: 10.0.0.50
          kafka_broker_id: 0
          kafka_advertised_hostname: 10.0.0.50
        kafka-node-1:
          ansible_host: 10.0.0.43
          kafka_broker_id: 1
          kafka_advertised_hostname: 10.0.0.43
#        kafka-node-2:
#          ansible_host: 10.0.0.60
#          kafka_broker_id: 2
#          kafka_advertised_hostname: 10.0.0.60

  vars:
    # This user will be used to connect to all servers
    ansible_user: "root"
